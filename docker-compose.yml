version: '3.8'

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-2020}:${PORT:-2020}"
    environment:
      - PORT=${PORT:-2020}
    restart: unless-stopped
